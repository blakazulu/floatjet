---
import { Image } from 'astro:assets';

// Articles grouped by category - 3 articles per category
const articlesByCategory = {
  saas: [
    {
      title: "Best Project Management Tools for Remote Teams",
      excerpt: "Boost productivity and stay organized with these essential SaaS platforms designed for distributed teams.",
      image: "https://images.unsplash.com/photo-1454165804606-c3d57bc86b40?auto=format&fit=crop&w=1200&q=80",
      slug: "best-project-management-tools",
      readTime: "8 min read"
    },
    {
      title: "Top CRM Software for Small Businesses",
      excerpt: "Manage customer relationships effectively with these powerful yet affordable CRM solutions.",
      image: "https://images.unsplash.com/photo-1552664730-d307ca884978?auto=format&fit=crop&w=1200&q=80",
      slug: "best-crm-software-small-business",
      readTime: "6 min read"
    },
    {
      title: "Best Team Communication Tools",
      excerpt: "Keep your remote team connected with these top-rated communication and collaboration platforms.",
      image: "https://images.unsplash.com/photo-1600880292203-757bb62b4baf?auto=format&fit=crop&w=1200&q=80",
      slug: "best-team-communication-tools",
      readTime: "7 min read"
    }
  ],
  vpn: [
    {
      title: "Best VPNs for Digital Nomads in 2024",
      excerpt: "Secure your connection anywhere in the world with our top picks for privacy, speed, and reliability.",
      image: "https://images.unsplash.com/photo-1614064641938-3bbee52942c7?auto=format&fit=crop&w=1200&q=80",
      slug: "best-vpns-digital-nomads",
      readTime: "10 min read"
    },
    {
      title: "VPN Speed Comparison: Which is Fastest?",
      excerpt: "We tested 15 popular VPNs to find which ones offer the best speeds for streaming and work.",
      image: "https://images.unsplash.com/photo-1558494949-ef010cbdcc31?auto=format&fit=crop&w=1200&q=80",
      slug: "vpn-speed-comparison",
      readTime: "12 min read"
    },
    {
      title: "How to Choose a VPN for Public WiFi",
      excerpt: "Stay safe on coffee shop and airport networks with these essential VPN features and recommendations.",
      image: "https://images.unsplash.com/photo-1521791136064-7986c2920216?auto=format&fit=crop&w=1200&q=80",
      slug: "vpn-public-wifi-guide",
      readTime: "5 min read"
    }
  ],
  hosting: [
    {
      title: "Best Web Hosting for Remote Businesses",
      excerpt: "Reliable hosting solutions with global reach, excellent uptime, and scalable resources.",
      image: "https://images.unsplash.com/photo-1558494949-ef010cbdcc31?auto=format&fit=crop&w=1200&q=80",
      slug: "best-web-hosting-remote-businesses",
      readTime: "9 min read"
    },
    {
      title: "Managed WordPress Hosting Compared",
      excerpt: "Find the perfect managed hosting for your WordPress site with our detailed comparison.",
      image: "https://images.unsplash.com/photo-1460925895917-afdab827c52f?auto=format&fit=crop&w=1200&q=80",
      slug: "managed-wordpress-hosting-compared",
      readTime: "11 min read"
    },
    {
      title: "Cloud Hosting vs Traditional: Which is Better?",
      excerpt: "Understanding the differences to make the right choice for your online presence.",
      image: "https://images.unsplash.com/photo-1544197150-b99a580bb7a8?auto=format&fit=crop&w=1200&q=80",
      slug: "cloud-vs-traditional-hosting",
      readTime: "7 min read"
    }
  ],
  gear: [
    {
      title: "Ultimate Home Office Setup Guide",
      excerpt: "Must-have gear to upgrade your workspace for maximum comfort, productivity, and ergonomics.",
      image: "https://images.unsplash.com/photo-1493934558415-9d19f0b2b4d2?auto=format&fit=crop&w=1200&q=80",
      slug: "ultimate-home-office-setup",
      readTime: "15 min read"
    },
    {
      title: "Best Laptops for Remote Work",
      excerpt: "Portable powerhouses that balance performance, battery life, and portability for nomadic professionals.",
      image: "https://images.unsplash.com/photo-1496181133206-80ce9b88a853?auto=format&fit=crop&w=1200&q=80",
      slug: "best-laptops-remote-work",
      readTime: "12 min read"
    },
    {
      title: "Essential Travel Tech for Digital Nomads",
      excerpt: "Compact gadgets and accessories that make working from anywhere a breeze.",
      image: "https://images.unsplash.com/photo-1488590528505-98d2b5aba04b?auto=format&fit=crop&w=1200&q=80",
      slug: "essential-travel-tech-nomads",
      readTime: "8 min read"
    }
  ],
  finance: [
    {
      title: "Best Banking for Digital Nomads",
      excerpt: "International banking solutions with low fees, multi-currency support, and global ATM access.",
      image: "https://images.unsplash.com/photo-1579621970563-ebec7560ff3e?auto=format&fit=crop&w=1200&q=80",
      slug: "best-banking-digital-nomads",
      readTime: "10 min read"
    },
    {
      title: "Crypto-Friendly Banks for Remote Workers",
      excerpt: "Traditional banks that play nice with cryptocurrency for the modern digital professional.",
      image: "https://images.unsplash.com/photo-1621761191319-c6fb62004040?auto=format&fit=crop&w=1200&q=80",
      slug: "crypto-friendly-banks",
      readTime: "8 min read"
    },
    {
      title: "Tax Tips for Location-Independent Professionals",
      excerpt: "Navigate international tax obligations and optimize your finances as a remote worker.",
      image: "https://images.unsplash.com/photo-1554224155-6726b3ff858f?auto=format&fit=crop&w=1200&q=80",
      slug: "tax-tips-remote-workers",
      readTime: "14 min read"
    }
  ],
  travel: [
    {
      title: "Best Travel Insurance for Remote Workers",
      excerpt: "Comprehensive coverage designed for long-term travelers and location-independent professionals.",
      image: "https://images.unsplash.com/photo-1488085061387-422e29b40080?auto=format&fit=crop&w=1200&q=80",
      slug: "best-travel-insurance-remote-workers",
      readTime: "9 min read"
    },
    {
      title: "Top Destinations for Digital Nomads in 2024",
      excerpt: "Cities with great WiFi, affordable living, and thriving remote work communities.",
      image: "https://images.unsplash.com/photo-1502920917128-1aa500764cbd?auto=format&fit=crop&w=1200&q=80",
      slug: "top-destinations-digital-nomads",
      readTime: "11 min read"
    },
    {
      title: "How to Find Reliable WiFi Anywhere",
      excerpt: "Tips and tools for staying connected no matter where your travels take you.",
      image: "https://images.unsplash.com/photo-1521295121783-8a321d551ad2?auto=format&fit=crop&w=1200&q=80",
      slug: "find-reliable-wifi-anywhere",
      readTime: "6 min read"
    }
  ]
};

const categories = ["saas", "vpn", "hosting", "gear", "finance", "travel"];
const categoryNames: Record<string, string> = {
  saas: "SaaS",
  vpn: "VPN",
  hosting: "Hosting",
  gear: "Gear",
  finance: "Finance",
  travel: "Travel"
};
---

<section class="py-4 md:py-6">
  <div class="container mx-auto px-4 md:px-6">
    <!-- Carousel Container -->
    <div class="relative">
      <!-- Left Arrow -->
      <button
        type="button"
        id="carousel-prev"
        class="absolute left-0 top-1/2 -translate-y-1/2 -translate-x-2 md:-translate-x-6 z-20 w-12 h-12 md:w-14 md:h-14 flex items-center justify-center rounded-full bg-white/10 backdrop-blur-md border border-white/20 text-white hover:bg-white/20 hover:scale-110 transition-all duration-300 shadow-lg"
        aria-label="Previous article"
      >
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
        </svg>
      </button>

      <!-- Articles Container -->
      <div class="overflow-hidden mx-8 md:mx-16">
        {categories.map((categoryId, catIndex) => (
          <div
            id={`carousel-${categoryId}`}
            data-category={categoryId}
            class:list={[
              "carousel-panel",
              catIndex === 0 ? "block" : "hidden"
            ]}
            role="tabpanel"
            aria-labelledby={`tab-${categoryId}`}
          >
            {articlesByCategory[categoryId as keyof typeof articlesByCategory].map((article, index) => (
              <article
                data-index={index}
                class:list={[
                  "carousel-slide transition-all duration-500",
                  index === 0 ? "block" : "hidden"
                ]}
              >
                <a href={`/blog/${article.slug}`} class="block group">
                  <div class="relative rounded-2xl overflow-hidden bg-white/10 backdrop-blur-md border border-white/20 shadow-xl">
                    <!-- Image -->
                    <div class="relative h-64 md:h-96 overflow-hidden">
                      <Image
                        src={article.image}
                        alt={article.title}
                        width={1200}
                        height={600}
                        class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700"
                        loading={catIndex === 0 && index === 0 ? "eager" : "lazy"}
                      />
                      <div class="absolute inset-0 bg-gradient-to-t from-ocean-deep/90 via-ocean-deep/40 to-transparent"></div>
                    </div>

                    <!-- Content Overlay -->
                    <div class="absolute bottom-0 left-0 right-0 p-6 md:p-10">
                      <span class="inline-block px-3 py-1 mb-4 text-xs font-medium bg-jet-stream/90 text-white rounded-full">
                        {categoryNames[categoryId]}
                      </span>
                      <h2 class="text-2xl md:text-4xl font-bold text-white mb-3 leading-tight group-hover:text-sky-light transition-colors">
                        {article.title}
                      </h2>
                      <p class="text-white/80 text-base md:text-lg mb-4 line-clamp-2 max-w-3xl">
                        {article.excerpt}
                      </p>
                      <div class="flex items-center gap-4 text-white/60 text-sm">
                        <span>{article.readTime}</span>
                        <span class="flex items-center gap-1 text-sky-light group-hover:gap-2 transition-all">
                          Read Article
                          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                          </svg>
                        </span>
                      </div>
                    </div>
                  </div>
                </a>
              </article>
            ))}

            <!-- Dots Indicator -->
            <div class="flex justify-center gap-2 mt-6">
              {articlesByCategory[categoryId as keyof typeof articlesByCategory].map((_, index) => (
                <button
                  type="button"
                  data-slide={index}
                  class:list={[
                    "carousel-dot w-2.5 h-2.5 rounded-full transition-all duration-300",
                    index === 0 ? "bg-jet-stream w-8" : "bg-white/30 hover:bg-white/50"
                  ]}
                  aria-label={`Go to slide ${index + 1}`}
                />
              ))}
            </div>
          </div>
        ))}
      </div>

      <!-- Right Arrow -->
      <button
        type="button"
        id="carousel-next"
        class="absolute right-0 top-1/2 -translate-y-1/2 translate-x-2 md:translate-x-6 z-20 w-12 h-12 md:w-14 md:h-14 flex items-center justify-center rounded-full bg-white/10 backdrop-blur-md border border-white/20 text-white hover:bg-white/20 hover:scale-110 transition-all duration-300 shadow-lg"
        aria-label="Next article"
      >
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
        </svg>
      </button>
    </div>

    <!-- More Articles Button -->
    <div class="text-center mt-8">
      <a
        id="more-articles-btn"
        href="/tools/saas"
        class="inline-flex items-center gap-2 px-8 py-3.5 rounded-xl bg-white/10 backdrop-blur-md border border-white/20 text-white font-medium hover:bg-jet-stream hover:border-jet-stream hover:shadow-lg transition-all duration-300"
      >
        <span>More articles from</span>
        <span id="selected-category-name" class="font-bold text-sky-light">SaaS</span>
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
      </a>
    </div>
  </div>
</section>

<script>
  // Category and carousel state
  let currentCategory = 'saas';
  let currentSlide = 0;

  const categoryNames: Record<string, string> = {
    saas: "SaaS",
    vpn: "VPN",
    hosting: "Hosting",
    gear: "Gear",
    finance: "Finance",
    travel: "Travel"
  };

  const categoryUrls: Record<string, string> = {
    saas: "/tools/saas",
    vpn: "/tools/vpn",
    hosting: "/tools/hosting",
    gear: "/gear",
    finance: "/tools/finance",
    travel: "/guides/travel"
  };

  // Initialize
  document.addEventListener('DOMContentLoaded', () => {
    setupCategoryTabs();
    setupCarouselNavigation();
    setupDotNavigation();
  });

  function setupCategoryTabs() {
    const tabs = document.querySelectorAll('.category-tab');
    tabs.forEach(tab => {
      tab.addEventListener('click', () => {
        const category = tab.getAttribute('data-category');
        if (category && category !== currentCategory) {
          switchCategory(category);
        }
      });
    });
  }

  function switchCategory(newCategory: string) {
    // Update tabs
    document.querySelectorAll('.category-tab').forEach(tab => {
      const isSelected = tab.getAttribute('data-category') === newCategory;
      tab.setAttribute('aria-selected', isSelected ? 'true' : 'false');
      if (isSelected) {
        tab.classList.remove('bg-white/10', 'text-white/80');
        tab.classList.add('bg-jet-stream', 'text-white', 'shadow-lg');
      } else {
        tab.classList.remove('bg-jet-stream', 'shadow-lg');
        tab.classList.add('bg-white/10', 'text-white/80');
      }
    });

    // Hide current panel, show new panel
    document.querySelectorAll('.carousel-panel').forEach(panel => {
      if (panel.getAttribute('data-category') === newCategory) {
        panel.classList.remove('hidden');
        panel.classList.add('block');
      } else {
        panel.classList.remove('block');
        panel.classList.add('hidden');
      }
    });

    // Reset to first slide
    currentCategory = newCategory;
    currentSlide = 0;
    updateSlide();
    updateMoreButton();
  }

  function setupCarouselNavigation() {
    const prevBtn = document.getElementById('carousel-prev');
    const nextBtn = document.getElementById('carousel-next');

    prevBtn?.addEventListener('click', () => {
      currentSlide = currentSlide === 0 ? 2 : currentSlide - 1;
      updateSlide();
    });

    nextBtn?.addEventListener('click', () => {
      currentSlide = currentSlide === 2 ? 0 : currentSlide + 1;
      updateSlide();
    });
  }

  function setupDotNavigation() {
    document.querySelectorAll('.carousel-dot').forEach(dot => {
      dot.addEventListener('click', () => {
        const slideIndex = parseInt(dot.getAttribute('data-slide') || '0');
        currentSlide = slideIndex;
        updateSlide();
      });
    });
  }

  function updateSlide() {
    const currentPanel = document.querySelector(`.carousel-panel[data-category="${currentCategory}"]`);
    if (!currentPanel) return;

    // Update slides
    currentPanel.querySelectorAll('.carousel-slide').forEach((slide, index) => {
      if (index === currentSlide) {
        slide.classList.remove('hidden');
        slide.classList.add('block');
      } else {
        slide.classList.remove('block');
        slide.classList.add('hidden');
      }
    });

    // Update dots
    currentPanel.querySelectorAll('.carousel-dot').forEach((dot, index) => {
      if (index === currentSlide) {
        dot.classList.remove('bg-white/30', 'w-2.5');
        dot.classList.add('bg-jet-stream', 'w-8');
      } else {
        dot.classList.remove('bg-jet-stream', 'w-8');
        dot.classList.add('bg-white/30', 'w-2.5');
      }
    });
  }

  function updateMoreButton() {
    const btn = document.getElementById('more-articles-btn') as HTMLAnchorElement;
    const categoryNameSpan = document.getElementById('selected-category-name');

    if (btn && categoryNameSpan) {
      btn.href = categoryUrls[currentCategory];
      categoryNameSpan.textContent = categoryNames[currentCategory];
    }
  }
</script>
