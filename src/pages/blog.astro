---
import HubPage from "@components/hub/HubPage.astro";
import { getArticlesBySection } from "@data/articles";
import { getSectionFilters, sections } from "@data/sections";

const config = sections.blog;
---

<HubPage
    metaTitle={config.metaTitle}
    metaDescription={config.metaDescription}
    heading={config.heading}
    subtitle={config.subtitle}
    note={config.note}
    countLabel={config.countLabel}
    articles={getArticlesBySection("blog")}
    filters={getSectionFilters("blog")}
    activeHref="/blog/"
    breadcrumbs={[{ label: "Home", href: "/" }, { label: config.label, href: "/blog/" }]}
/>
