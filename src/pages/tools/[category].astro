---
import HubPage from "@components/hub/HubPage.astro";
import { getArticlesByCategory } from "@data/articles";
import { getSectionFilters, sections, type CategoryConfig } from "@data/sections";

export function getStaticPaths() {
  return sections.tools.categories.map((category) => ({
    params: { category: category.slug },
    props: { category },
  }));
}

const { category } = Astro.props as { category: CategoryConfig };
const config = sections.tools;
---

<HubPage
    metaTitle={`${category.title} | FloatJet`}
    metaDescription={category.description}
    heading={category.title}
    subtitle={category.subtitle}
    intro={category.intro}
    startHere={category.startHere}
    note={config.note}
    countLabel={config.countLabel}
    articles={getArticlesByCategory("tools", category.slug)}
    filters={getSectionFilters("tools")}
    activeHref={`/tools/${category.slug}/`}
    breadcrumbs={[
      { label: "Home", href: "/" },
      { label: config.label, href: "/tools/" },
      { label: category.name, href: `/tools/${category.slug}/` },
    ]}
/>
